version: '3.8'

services:
  kanban:
    build: .
    ports:
      - "6001:6001"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
    env_file:
      - .env
    restart: unless-stopped
